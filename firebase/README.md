# Firebase + dummyoauth

Companion OIDC client (not Firebase UI buttons).

## Setup

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

Redirect URI: http://localhost:3008/callback

Development and test only.
