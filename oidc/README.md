# OIDC example

Vanilla OAuth 2.0 / OIDC authorization code + PKCE against dummyoauth.

## Setup

```bash
cp .env.example .env.local
pnpm install
pnpm dev
```

Redirect URI: http://localhost:3001/callback

Development and test only.
